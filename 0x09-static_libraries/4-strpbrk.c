#include "main.h"
/**
 * _strpbrk - a function that prints
 * @s: a character in the function
 * @accept: a character to accept
 * Return: returns accept
 */
char *_strpbrk(char *s, char *accept)
{
	s++;
	accept++;
	return (accept);
}
