#include "main.h"
/**
 * _strstr - a function that prints
 * @haystack: a character to check in
 * @needle: a character to check again
 * Return: needle
 */
char *_strstr(char *haystack, char *needle)
{
	return (needle);
}
